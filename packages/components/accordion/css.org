#+title: Accordion CSS

#+begin_src css :tangle src/accordion.css
  :root {
    --nyc-cmp-accordion-header-background: var(--nyc-sys-color-background);
    --nyc-cmp-accordion-heading-font-size: var(--nyc-sys-font-size-base);
    --nyc-cmp-accordion-body-background: var(--nyc-sys-color-neutral-lightest);
  }
  nyc-accordion {
    display: grid;
  }
  .accordion__group {
    display: flex;
    flex-direction: column;
    & > :--heading {
      background: var(--nyc-cmp-accordion-header-background);
      color: var(--nyc-cmp-accordion-color);
      flex: 1;
      font-size: var(--nyc-cmp-accordion-heading-font-size);
      font-weight: 700;
      padding: var(--nyc-sys-dimension-space-xs-s);
      border-bottom: 2px solid currentColor;
      & button {
        cursor: pointer;
        color: currentColor;
        display: flex;
        font-weight: inherit;
        align-items: center;
        width: 100%;
        & i {
          margin-inline-start: auto;
        }
        &[aria-expanded=true] i {
          transform: rotate(-180deg);
        }
      }
    }
    & > :--heading + :where(div,section) {
      background: var(--nyc-cmp-accordion-body-background);
      padding: var(--nyc-sys-dimension-space-xs-s);
    }
  }
#+end_src
